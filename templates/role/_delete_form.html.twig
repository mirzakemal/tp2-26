<form method="post" action="{{ path('app_role_delete', {'id': role.idRole}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.idRole) }}">
    <button class="btn">Delete</button>
</form>
